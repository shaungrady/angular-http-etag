engines:
  eslint:
    enabled: true
    channel: "eslint-2"
    config:
      config: .eslintrc.json

ratings:
  paths:
  - "src/**/*.js"

ecmaFeatures:
  modules: true

env:
  amd: true
  browser: true
