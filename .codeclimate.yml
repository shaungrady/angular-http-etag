engines:
  eslint:
    enabled: true
    channel: "eslint-3"
    config:
      config: .eslintrc.json
    exlude_paths:
    - "test/"

ratings:
  paths:
  - "src/**/*.js"

ecmaFeatures:
  modules: true

env:
  amd: true
  browser: true
